import 'package:flutter/material.dart';

class Pag3 extends StatelessWidget {
  const Pag3({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("3", selectionColor: Colors.black,));
  }
}